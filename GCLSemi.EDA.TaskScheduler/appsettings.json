{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "Quartz": {
    "Database": "USER ID=eda;PASSWORD=eda01;DATA SOURCE=\"(DESCRIPTION=(ADDRESS_LIST=(ADDRESS=(PROTOCOL=TCP)(HOST=10.48.68.164)(PORT=1521)))(CONNECT_DATA=(SERVICE_NAME=gclrpt)))\"", // quartz 持久化数据库连接，本实例使用MySQL
    "TablePrefix": "QRTZ_" // MySQL下注意区分大小写
  }
}
